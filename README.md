# Nifty50_Predication
This project predicts the Nifty 50 index’s next closing price using machine learning regression. Historical Nifty 50 data — including daily Open, High, Low, Last, Previous Close, and VWAP — is used to train a predictive model. The trained model is deployed using  Flask web application, where users can input stock data and get an instant prediction.
